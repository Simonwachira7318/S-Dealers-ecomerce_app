# S-Dealers-ecomerce_app

client dev
npx @react-native-community/cli init sdealers


server dev:
npm init -y
npm install express mysql2 bcryptjs jsonwebtoken dotenv cors nodemailer multer body-parser nodemon

structure:

